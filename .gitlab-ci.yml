image: node:latest


cache:
  paths:
  - node_modules/

stages:
  - pages
  - test 
  

pages:
  script:
    - npm install
    - ./node_modules/.bin/gatsby build --prefix-paths
  artifacts:
    paths:
      - public
  only:
    - master
test:     
  stage: test     
  script:         
    - npm install
    - npm run test